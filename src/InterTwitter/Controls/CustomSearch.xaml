<?xml version="1.0" encoding="utf-8" ?>
<Grid
    x:Class="InterTwitter.Controls.CustomSearch"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    x:Name="customSearch">

    <Frame
        Padding="0,-10,0,0"
        BackgroundColor="{StaticResource Light Grey}"
        CornerRadius="20"
        HasShadow="False"
        HeightRequest="48">
        <Entry
            Margin="5,0,0,-10"
            HorizontalOptions="FillAndExpand"
            Style="{StaticResource EntryCaption}"
            Text="{Binding Text}"
            VerticalOptions="FillAndExpand">
            <Entry.Behaviors>
                <prism:EventToCommandBehavior Command="{Binding TextChangedCommand, Source={x:Reference customSearch}}" EventName="TextChanged" />
            </Entry.Behaviors>
        </Entry>
    </Frame>

</Grid>